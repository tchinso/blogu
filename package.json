{
  "name": "blogu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bundle exec jekyll clean && bundle exec jekyll build && node scripts/verify-static-output.mjs",
    "build:cloudflare": "bundle install && bundle exec jekyll clean && bundle exec jekyll build --config _config.yml,_config.cloudflare.yml && node scripts/verify-static-output.mjs",
    "build:github-preview": "bundle exec jekyll clean && bundle exec jekyll build --config _config.yml && node scripts/verify-static-output.mjs",
    "verify": "node scripts/verify-static-output.mjs"
  }
}
